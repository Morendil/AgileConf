<h1><%= @session.title %></h1>
<div>
    <p>Agile <%= @session.year %> / <%= @session.type %> / <%= @session.stage %></p>
    <p><%= @session.description.gsub("\\n","<br/>") %></p>
    <p><%= @session.speakers.map(&:name).join(", ") %></p>
</div>
